# AI-генерация бэклога

AI Backlog Generation анализирует код репозитория и автоматически создаёт задачи на улучшение.

## Обзор

ИИ сканирует весь репозиторий и предлагает улучшения в виде задач по следующим направлениям:

- **Безопасность** — Уязвимости и рискованные паттерны
- **Производительность** — Узкие места и неэффективный код
- **Качество кода** — Дублирование, функции с высокой сложностью
- **Тестирование** — Области с низким покрытием тестами
- **Документация** — Отсутствующая документация

## Использование

### Из панели управления

1. Выберите целевой репозиторий
2. Перейдите на вкладку **Backlog**
3. Нажмите **Generate Backlog**
4. ИИ анализирует репозиторий (может занять несколько минут)
5. Просмотрите сгенерированные предложения
6. Выберите нужные и нажмите **Create Issues**

### Через API

```bash
# Запуск генерации бэклога
POST /api/github/{owner}/{repo}/backlog/generate

# Получение результатов
GET /api/github/{owner}/{repo}/backlog
```

## Двухпроходный анализ

Генерация бэклога использует двухэтапный процесс:

### Проход 1: Широкое сканирование
- Карта файловой структуры репозитория
- Высокоуровневый анализ ключевых файлов
- Определение стека технологий и архитектуры

### Проход 2: Глубокий анализ
- Глубокое погружение в области фокуса по результатам Прохода 1
- Предложения на уровне строк кода
- Присвоение приоритетов (High / Medium / Low)

## Области фокуса

Настройте области анализа:

```yaml
repos:
  - name: my-repo
    workers:
      autopilot:
        focus:
          - security
          - performance
          - test-coverage
```

| Фокус | Описание |
|-------|----------|
| `security` | Уязвимости и риски безопасности |
| `performance` | Возможности повышения производительности |
| `code-quality` | Качество и читаемость кода |
| `test-coverage` | Улучшение покрытия тестами |
| `documentation` | Пробелы в документации |
| `accessibility` | Улучшения доступности |
| `refactoring` | Предложения по рефакторингу |

## Пользовательские промпты

Добавьте собственные критерии анализа:

```yaml
repos:
  - name: my-repo
    workers:
      autopilot:
        focus:
          - "Check if API response formats are consistent"
          - "Verify error handling follows best practices"
```

## Формат сгенерированных задач

Задачи, сгенерированные ИИ, содержат следующую информацию:

```markdown
## Проблема
Логика аутентификации в `src/services/auth.ts` не реализует
проверку срока действия JWT-токена.

## Влияние
Просроченные токены могут предоставлять доступ, создавая угрозу безопасности.

## Предлагаемое исправление
- Добавить проверку срока действия в функцию `validateToken()`
- Возвращать 401 Unauthorized для просроченных токенов

## Целевые файлы
- `src/services/auth.ts` (L45-L60)
- `src/middleware/auth.middleware.ts` (L12)

## Приоритет
🔴 Высокий
```

> [!TIP]
> Сгенерированные задачи автоматически получают метку `ados`, поэтому в сочетании с режимом AutoPilot весь процесс от предложения до реализации полностью автоматизируется.
